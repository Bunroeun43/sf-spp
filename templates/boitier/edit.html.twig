{% extends 'base.html.twig' %}

{% block title %}Editer un boitier
{% endblock %}

{% block body %}
	<div class="page boitier">
		<h1>Editer un boitier</h1>

		{{ include('boitier/_form.html.twig', {'button_label': 'mettre à jour'}) }}

		<a href="{{ path('app_boitier_index') }}" class="btn btn-primary">retour à la liste</a>

		{{ include('boitier/_delete_form.html.twig') }}
	</div>

{% endblock %}
