{% extends 'base.html.twig' %}

{% block title %}Edit Montage
{% endblock %}

{% block body %}
	<div class="page">
		<h1>Edit Montage</h1>

		{{ include('montage/_form.html.twig', {'button_label': 'mettre à jour'}) }}


		<a href="{{ path('app_montage_index') }}" class="btn btn-primary">retour à la liste</a>


		{{ include('montage/_delete_form.html.twig') }}
	</div>

{% endblock %}
