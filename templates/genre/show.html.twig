{% extends 'base.html.twig' %}

{% block title %}Genre
{% endblock %}

{% block body %}
	<div class="page genre">
		<h1>Genre</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ genre.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ genre.nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_genre_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_genre_edit', {'id': genre.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('genre/_delete_form.html.twig') }}
	</div>

{% endblock %}
