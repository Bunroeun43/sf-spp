{% extends 'base.html.twig' %}

{% block title %}Socket
{% endblock %}

{% block body %}
	<div class="page socket">
		<h1>Socket</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ socket.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ socket.nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_socket_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_socket_edit', {'id': socket.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('socket/_delete_form.html.twig') }}
	</div>

{% endblock %}
