{% extends 'base.html.twig' %}

{% block title %}Marque
{% endblock %}

{% block body %}
	<div class="page marque">
		<h1>Marque</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ marque.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ marque.nom }}</td>
				</tr>
				<tr>
					<th>Photo</th>
					<td>{{ marque.photo }}</td>
				</tr>
				<tr>
					<th>IsActive</th>
					<td>{{ marque.isActive ? 'Yes' : 'No' }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_marque_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_marque_edit', {'id': marque.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('marque/_delete_form.html.twig') }}
	</div>

{% endblock %}
