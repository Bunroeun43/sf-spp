<form method="post" action="{{ path('app_disque_dur_delete', {'id': disque_dur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disque_dur.id) }}">
	<button class="btn btn-danger">supprimer</button>

</form>
