{% extends 'base.html.twig' %}

{% block title %}DisqueDur
{% endblock %}

{% block body %}
	<div class="page hdd">
		<h1>DisqueDur</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ disque_dur.id }}</td>
				</tr>
				<tr>
					<th>Modele</th>
					<td>{{ disque_dur.modele }}</td>
				</tr>
				<tr>
					<th>Photo</th>
					<td>{{ disque_dur.photo }}</td>
				</tr>
				<tr>
					<th>Prix</th>
					<td>{{ disque_dur.prix }}</td>
				</tr>
				<tr>
					<th>IsActive</th>
					<td>{{ disque_dur.isActive ? 'Yes' : 'No' }}</td>
				</tr>
				<tr>
					<th>Capacite</th>
					<td>{{ disque_dur.capacite }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_disque_dur_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_disque_dur_edit', {'id': disque_dur.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('disque_dur/_delete_form.html.twig') }}
	</div>

{% endblock %}
