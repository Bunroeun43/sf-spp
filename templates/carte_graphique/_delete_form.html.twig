<form method="post" action="{{ path('app_carte_graphique_delete', {'id': carte_graphique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_graphique.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
