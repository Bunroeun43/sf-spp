{% extends 'base.html.twig' %}

{% block title %}Configuration
{% endblock %}

{% block body %}
	<div class="page alimentation">
		<h1>Configuration</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ configurateur.id }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_configurateur_index') }}" class="btn btn-primary btn-crud">retour à la liste</a>

		<a href="{{ path('app_configurateur_edit', {'id': configurateur.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('configurateur/_delete_form.html.twig') }}
	</div>

{% endblock %}
