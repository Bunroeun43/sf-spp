<form method="post" action="{{ path('app_pcie_delete', {'id': pcie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pcie.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
