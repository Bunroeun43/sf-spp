{% extends 'base.html.twig' %}

{% block title %}DisqueM2
{% endblock %}

{% block body %}
	<div class="page m2">
		<h1>DisqueM2</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ disque_m2.id }}</td>
				</tr>
				<tr>
					<th>Modele</th>
					<td>{{ disque_m2.modele }}</td>
				</tr>
				<tr>
					<th>Capacite</th>
					<td>{{ disque_m2.capacite }}</td>
				</tr>
				<tr>
					<th>IsActive</th>
					<td>{{ disque_m2.isActive ? 'Yes' : 'No' }}</td>
				</tr>
				<tr>
					<th>Photo</th>
					<td>{{ disque_m2.photo }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_disque_m2_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_disque_m2_edit', {'id': disque_m2.id}) }}" class="btn btn-warning">modifier</a>

		{{ include('disque_m2/_delete_form.html.twig') }}
	</div>

{% endblock %}
