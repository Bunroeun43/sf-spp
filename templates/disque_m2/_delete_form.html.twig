<form method="post" action="{{ path('app_disque_m2_delete', {'id': disque_m2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disque_m2.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
