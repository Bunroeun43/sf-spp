{% extends 'base.html.twig' %}

{% block title %}FormatDisque
{% endblock %}

{% block body %}
	<div class="page formatdisque">
		<h1>FormatDisque</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ format_disque.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ format_disque.nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_format_disque_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_format_disque_edit', {'id': format_disque.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('format_disque/_delete_form.html.twig') }}
	</div>

{% endblock %}
