{% extends 'base.html.twig' %}

{% block title %}Pays
{% endblock %}

{% block body %}
	<div class="page pays">
		<h1>Pays</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ pay.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ pay.nom }}</td>
				</tr>
				<tr>
					<th>Photo</th>
					<td>{{ pay.photo }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_pays_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_pays_edit', {'id': pay.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('pays/_delete_form.html.twig') }}
	</div>

{% endblock %}
