{% extends 'base.html.twig' %}

{% block title %}FormatBoitier
{% endblock %}

{% block body %}
	<div class="page formatboitier">
		<h1>FormatBoitier</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ format_boitier.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ format_boitier.nom }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_format_boitier_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_format_boitier_edit', {'id': format_boitier.id}) }}" class="btn btn-warning btn-crud">modifier</a>


		{{ include('format_boitier/_delete_form.html.twig') }}
	</div>

{% endblock %}
