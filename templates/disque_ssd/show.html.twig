{% extends 'base.html.twig' %}

{% block title %}DisqueSsd
{% endblock %}

{% block body %}
	<div class="page ssd">
		<h1>DisqueSsd</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ disque_ssd.id }}</td>
				</tr>
				<tr>
					<th>Modele</th>
					<td>{{ disque_ssd.modele }}</td>
				</tr>
				<tr>
					<th>Photo</th>
					<td>{{ disque_ssd.photo }}</td>
				</tr>
				<tr>
					<th>Prix</th>
					<td>{{ disque_ssd.prix }}</td>
				</tr>
				<tr>
					<th>IsActive</th>
					<td>{{ disque_ssd.isActive ? 'Yes' : 'No' }}</td>
				</tr>
				<tr>
					<th>Capacite</th>
					<td>{{ disque_ssd.capacite }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_disque_ssd_index') }}" class="btn btn-primary">retour à la liste</a>

		<a href="{{ path('app_disque_ssd_edit', {'id': disque_ssd.id}) }}" class="btn btn-warning btn-crud">modifier</a>

		{{ include('disque_ssd/_delete_form.html.twig') }}
	</div>

{% endblock %}
