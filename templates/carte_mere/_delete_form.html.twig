<form method="post" action="{{ path('app_carte_mere_delete', {'id': carte_mere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_mere.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
