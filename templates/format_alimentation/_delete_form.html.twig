<form method="post" action="{{ path('app_format_alimentation_delete', {'id': format_alimentation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ format_alimentation.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
